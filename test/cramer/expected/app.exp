fun = map double ints

double = (*) 2

printAll = foldr (\a b -> do
                      putStrLn a
                      b)
                 (return ())

commented = foo bar -- first arg
                baz -- second arg
