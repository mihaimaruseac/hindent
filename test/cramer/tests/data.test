data Empty = Empty

data Maybe a = Nothing | Just a

data Either a b = Left a | Right b

data Op a b = Op (b -> a)

data Enum = Foo | Bar | Baz

data Enum = Foo -- first
  | Bar -- second
  | Baz -- third
